# Copy this file to .env and fill in your values.

# Google Gemini API key (required for generation).
GEMINI_API_KEY=

# Gemini models to use (optional).
# Default is fast and cheaper; advanced is used when the UI option is enabled.
# Fallback is used only when the default model returns 429.
GEMINI_DEFAULT_MODEL=gemini-3.5-flash-lite
GEMINI_ADVANCED_MODEL=gemini-3.6-flash
GEMINI_FALLBACK_MODEL=gemini-3.1-flash-lite

# JWT signing secret (required). Use a long random string in production.
JWT_SECRET=change-me-with-a-long-random-secret
JWT_TTL=24h

# Postgres credentials (optional, sensible defaults).
POSTGRES_USER=prepai
POSTGRES_PASSWORD=prepai
POSTGRES_DB=prepai

# Host port the web app is exposed on (optional, defaults to 8080).
WEB_PORT=8080

# Comma-separated allowed CORS origins for the backend (optional).
ALLOWED_ORIGINS=http://localhost:8080
